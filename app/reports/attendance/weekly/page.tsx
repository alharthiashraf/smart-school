"use client";

import MonthlyAttendancePage from "../monthly/page";

/**
 * تقرير الحضور الأسبوعي
 * يمكن لاحقًا تخصيصه بفلاتر أسبوعية مستقلة،
 * لكنه يستخدم حالياً نفس الواجهة الاحترافية الموحدة.
 */
export default function WeeklyAttendancePage() {
  return <MonthlyAttendancePage />;
}

